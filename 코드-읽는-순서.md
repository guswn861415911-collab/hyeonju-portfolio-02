# 코드 읽는 순서 — 처음 수정하는 분을 위한 안내

이 프로젝트는 HTML(내용), CSS(모양), JavaScript(동작)로 만들어져 있습니다.
디자인과 기능을 그대로 두고 읽기 쉽게 정리했습니다. 복잡한 모달·영상 동작까지 없앤 것은 아닙니다.

## 1. 먼저 어떤 파일을 열까요?

| 바꾸고 싶은 곳 | 내용 | 모양 | 동작 |
| --- | --- | --- | --- |
| Home | index.html | style.css, home-mobile.css | script.js, portfolio-motion.js |
| About | about.html | about-profile.css, about-compact.css | about-profile.js |
| Projects 폴더·프로젝트 설명 | work.html | style.css, projects-mobile.css | script.js |
| 디핑북 모달 | work.html | dippingbook-modal.css | dippingbook-modal.js |
| 나머지 모바일 모달 | work.html | modals-mobile.css | modals-mobile.js |
| PC Me | contact.html | me.css | script.js |
| 모바일 Me | me-mobile.js 안의 groups와 HTML 문자열 | me-mobile.css | me-mobile.js |
| 공통 움직임 | — | portfolio-motion.css | portfolio-motion.js |
| 모바일 영상 자동 미리보기 | — | projects-mobile.css | projects-mobile-preview.js |
| 모바일 제목·상단 간격 | — | tab-headings.css | — |

`project.html`과 `skills.html`은 별도로 남아 있는 화면입니다. 메인 Me 메뉴는 `contact.html`입니다.

## 2. HTML을 읽는 방법

- `<h1>`, `<h2>`: 큰 제목입니다.
- `<p>`: 설명 문장입니다.
- `<img src="...">`: 이미지 파일 위치입니다.
- `<a href="...">`: 누르면 이동하는 주소입니다.
- `<button>`: JavaScript가 동작을 연결하는 버튼입니다.
- `class`: CSS가 모양을 지정할 때 사용하는 이름입니다.
- `id`, `data-*`: JavaScript가 요소를 찾을 때 사용합니다. 이름을 바꾸면 연결도 같이 바꿔야 합니다.

텍스트만 바꿀 때는 태그 안의 문장만 고쳐 보세요. 먼저 파일을 저장한 뒤 브라우저를 새로고침합니다.

## 3. CSS를 읽는 방법

```css
.example {
  font-size: 16px; /* 글자 크기 */
  padding: 20px;   /* 안쪽 여백 */
  margin: 12px;    /* 바깥쪽 여백 */
}
```

`@media (max-width: 767px)` 안은 모바일에만 적용됩니다.
`transition`은 상태가 바뀔 때의 움직임, `animation`은 정해 둔 움직임입니다.
`ms`는 시간 단위이며 1000ms가 1초입니다.

CSS는 뒤에 나온 규칙이 앞의 규칙을 덮어쓰기도 합니다. 단, 선택자의 우선순위와 `!important`가 먼저 고려됩니다.
현재 파일 뒤쪽에는 이전 요청을 반영한 보정 규칙이 있으므로, 같은 이름을 검색해 마지막 규칙까지 확인하세요.
이번 정리에서는 화면이 바뀌지 않도록 이 순서를 유지했습니다.

## 4. JavaScript를 읽는 방법

- `const`: 사용할 값을 이름 붙여 보관합니다.
- `let`: 나중에 바뀔 값을 보관합니다.
- `function`: 여러 동작을 묶어 이름을 붙입니다.
- `if`: 조건에 맞을 때만 실행합니다.
- `for ... of`, `forEach`: 여러 요소에 같은 일을 반복합니다.
- `querySelector`: 화면에서 요소 하나를 찾습니다.
- `querySelectorAll`: 해당하는 요소를 모두 찾습니다.
- `addEventListener`: 클릭이나 스크롤이 일어났을 때 할 일을 연결합니다.
- `classList`: CSS 이름을 붙이거나 뗍니다.
- `=>`: 함수를 짧게 적는 표현입니다. 코드 정리 후에도 일부 남아 있습니다.
- `?.`: 요소가 없으면 오류 대신 건너뜁니다.
- `Promise`, `async`, `await`: 이미지·영상 준비처럼 시간이 걸리는 일을 기다릴 때 사용합니다.

처음에는 `modals-mobile.js`를 읽어 보세요. 요소 찾기 → 원래 자리 기록 → 화면 크기에 맞춰 이동 순서로 풀어 썼습니다.
그 다음 `script.js` 맨 위의 setup 목록에서 필요한 기능으로 이동하면 됩니다.

## 5. 자주 바꾸는 곳

- 모바일 Me 도구 목록: `me-mobile.js`의 `groups`
- 프로젝트 05~08 설명: `script.js`에서 해당 프로젝트 제목 검색
- 굿노트 이미지 목록: `work.html`의 `data-src`
- 모션 속도: `portfolio-motion.js`의 `duration`, `delay` 또는 CSS의 `transition`
- 이미지 경로는 파일 이름과 대소문자까지 정확히 맞춰 주세요.

## 6. 확인 방법

1. 한 번에 한 부분만 바꿉니다.
2. PC와 모바일 너비에서 확인합니다.
3. 모달 열기·닫기, 페이지 넘김, 탭 전환과 링크를 눌러 봅니다.
4. 브라우저 개발자 도구의 Console에서 빨간 오류를 확인합니다.

JavaScript 문법은 `node --check 파일이름.js`로 확인할 수 있습니다.
`.prettierrc.json`은 들여쓰기와 줄바꿈 규칙입니다. 사이트 실행에 별도 설치는 필요하지 않습니다.
`.code-before-cleanup`에는 이번 정리 직전 파일이 보관되어 있습니다. 웹페이지에서 사용하지 않는 백업입니다.

실제 브라우저 동작까지 자동 검증한 것은 아니므로, 디자인 수정 후에는 화면에서도 확인해 주세요.
